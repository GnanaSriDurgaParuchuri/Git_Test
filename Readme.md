# Git Test

This repository contains simple files for testing Git commands.

Quick commands:
```
git init
git add index.html Readme.md
git commit -m "Add test files"
git status
git log --oneline
```

Open index.html in a browser to view the page.
